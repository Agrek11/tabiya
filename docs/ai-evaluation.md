# AI evaluation status

Stockfish output and deterministic feature extraction are separate from optional model narration. Schema validation can establish shape; citation validation can establish that a cited feature exists. Neither proves a free-form explanation is chess-correct.

Run `npm run test:unit` for the automated fixture suite. Provider failures must leave the trainer usable through deterministic engine output. The Coach is experimental; tactical claims should be checked against the engine. Prompt and cache versions must be part of cache identity.

The manual ten-position walkthrough is incomplete. Do not market the Coach as grounded or reliable until it is completed and reviewed by a human chess expert.
