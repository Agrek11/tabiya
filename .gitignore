# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets
.env
.env.*
!.env.example

# Python
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Catalog build cache
scripts/.cache/

# Phase 2a — key-squares pipeline intermediates (regenerable; never committed).
# scraped/ + pending/ + .review_state.json are reviewer state, not source of truth.
# rejected/ is git-tracked deliberately as a prompt-tuning corpus (see R3.7).
data/key_squares/scraped/
data/key_squares/pending/
data/key_squares/.review_state.json

# Design screenshots — kept locally for reference, not in repo
Screenshot*.png
Screenshot*.jpg
Screenshot*.jpeg

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
# Wrangler local state
.wrangler/
